# Pi 0.80.6 can reach npmjs before the configured enterprise mirror catches up.
@earendil-works:registry=https://registry.npmjs.org/
# Keep lockfile integrity URLs reproducible instead of rewriting npmjs to the ambient registry.
replace-registry-host=never
