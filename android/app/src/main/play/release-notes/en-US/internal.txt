Pi Droid preview 5 makes multi-host daily use safer: edit, re-pair, forget, and crash-safe recovery. Create or adopt sessions, then observe, request control, wake, and stream them through the reviewed lifecycle.

Process-death resume restores accepted work as indeterminate and never replays it. The missing accessibility label is restored.
